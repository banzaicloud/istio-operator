module: github.com/banzaicloud/istio-operator/v2

openapi:
  selfContained: true
  fieldFilter: "min.*|max.*"

directories:
  api/v1alpha1:
    - mode: perFile

all:
  title: OpenAPI descriptor for Istio operator types
  oapiFilename: api/v1alpha1/istio-operator.gen.json

crd:
  dir: config/crd/bases
  filename: istio-operator-crds
  maxDescriptionLength: 0
